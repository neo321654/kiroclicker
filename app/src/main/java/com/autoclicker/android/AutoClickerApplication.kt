package com.autoclicker.android

import android.app.Application

/**
 * Application class for the AutoClicker app.
 * Handles application-level initialization and configuration.
 */
class AutoClickerApplication : Application() {

    override fun onCreate() {
        super.onCreate()
        
        // Initialize application-level components if needed
        // For example: crash reporting, analytics, etc.
    }
}